services:
  - type: web
    name: gq-electrical
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "3000"
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
